[pytest]
# This ensures logs are captured for the HTML report
log_level = INFO
log_format = %(asctime)s [%(levelname)s] %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

# This ensures logs still show up in your terminal
log_cli = true
log_cli_level = INFO